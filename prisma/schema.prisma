// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}


model Product {
  id                    String          @id @default(uuid())
  title                 String
  description           String?         @db.Text // Може бути довший опис
  price                 Decimal         @db.Decimal(10, 2)
  currency              String          @db.VarChar(10)

  // Нові поля для матеріалів та виробництва
  primaryMaterial       String?         // Дуб, Бук, Ялина
  secondaryMaterials    String[]        // @db.String[] - Якщо Prisma підтримує масиви, інакше JSON або окрема таблиця
  woodTreatment         String?         // Натуральне масло, Лак
  finish                String?         // Матовий, Глянцевий
  originOfMaterial      String?         // Карпатська деревина
  handmade              Boolean         @default(true) // Зазвичай true для кастомних
  craftsmanshipDetails  String[]        // Різьблення, Інкрустація

  width                 Decimal?        @db.Decimal(10, 2)
  depth                 Decimal?        @db.Decimal(10, 2)
  height                Decimal?        @db.Decimal(10, 2)
  unitOfMeasure         String?         @db.VarChar(10) 
  weight                Decimal?        @db.Decimal(10, 2)
  weightUnit            String?         @db.VarChar(10) 
  seatingCapacity       Int?
  storageCapacity       String?         

  style                 String?       
  color                 String?         
  texture               String?        
  designInspiration     String?
  pattern               String?

  features              String[]        
  assemblyRequired      Boolean?
  careInstructions      String?         @db.Text
  warranty              String?

  uniqueIdentifier      String?         @unique 
  designer              String?
  creationDate          DateTime?      
  story                 String?         @db.Text

  createdAt             DateTime        @default(now())
  updatedAt             DateTime        @updatedAt

  // Relations
  category              Category        @relation(fields: [categoryId], references: [id])
  categoryId            String
  images                ProductImage[]
}

model Category {
  id       String     @id @default(uuid())
  name     String     @unique
  categoryImage String?
  products Product[]
}

model ProductImage {
  id        String   @id @default(uuid())
  url       String
  altText   String?
  isMain    Boolean  @default(false)
  createdAt DateTime @default(now())

  // Relations
  product   Product  @relation(fields: [productId], references: [id])
  productId String
}
